spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:latest:///postgres
  liquibase:
    change-log: db/changelog.xml

logging:
  level:
    booster.webserver: TRACE
